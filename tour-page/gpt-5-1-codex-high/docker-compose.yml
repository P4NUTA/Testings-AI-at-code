version: "3.9"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD-SHELL", "node /app/src/healthcheck.js"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
